//
//  ContentView.swift
//  CatsApp
//
//  Created by user935646 on 13.03.24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}

u3r?<wm*Vzc@L4gB

u3r?<wm*Vzc@L4gB

√çç««««u3r?<wm*Vzc@L4gB
c@L4gB
u3r?<wm*Vzc@L4gB
